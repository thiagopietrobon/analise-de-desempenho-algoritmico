const CSV_URL = '../dados/quicksort_resultados.csv';
const $ = (id) => document.getElementById(id);
let rows = [], chart, labChart;
const palette = ['#60a5fa','#4ade80','#fbbf24','#c084fc'];
const scenarios = [
  {key:'Fixo|Aleatório', label:'Pivô fixo · entrada aleatória'},
  {key:'Fixo|Ordenado', label:'Pivô fixo · entrada ordenada'},
  {key:'Aleatório|Aleatório', label:'Pivô aleatório · entrada aleatória'},
  {key:'Aleatório|Ordenado', label:'Pivô aleatório · entrada ordenada'}
];
function parseCSV(text){
  const lines=text.trim().split(/\r?\n/); const headers=lines.shift().split(',');
  return lines.map(line=>{const values=line.split(',');const obj=Object.fromEntries(headers.map((h,i)=>[h,values[i]]));return {n:Number(obj.tamanho_n),algorithm:obj.algoritmo,scenario:obj.cenario,time:Number(obj.tempo_segundos),growth:obj.razao_crescimento===''?null:Number(obj.razao_crescimento)};});
}
function filtered(){return rows.filter(r=>($('algorithm').value==='all'||r.algorithm===$('algorithm').value)&&($('scenario').value==='all'||r.scenario===$('scenario').value));}
function render(){const data=filtered();$('resultsBody').innerHTML=data.map(r=>`<tr><td>${r.n.toLocaleString('pt-BR')}</td><td>${r.algorithm}</td><td>${r.scenario}</td><td>${r.time.toFixed(6)}</td><td>${r.growth===null?'—':r.growth.toFixed(2)+'×'}</td></tr>`).join('');$('tableStatus').textContent=`Exibindo ${data.length} de ${rows.length} medições.`;
 const keys=[...new Set(data.map(r=>r.algorithm+'|'+r.scenario))]; const datasets=keys.map((key,i)=>{const [a,s]=key.split('|');return {label:`Pivô ${a.toLowerCase()} · entrada ${s.toLowerCase()}`,data:data.filter(r=>r.algorithm===a&&r.scenario===s).sort((x,y)=>x.n-y.n).map(r=>({x:r.n,y:r.time})),borderColor:palette[scenarios.findIndex(v=>v.key===key)%palette.length],backgroundColor:palette[scenarios.findIndex(v=>v.key===key)%palette.length],tension:.2};});
 if(chart)chart.destroy();chart=new Chart($('resultsChart'),{type:'line',data:{datasets},options:{responsive:true,maintainAspectRatio:false,interaction:{mode:'nearest',intersect:false},scales:{x:{type:'linear',title:{display:true,text:'Tamanho da entrada (N)',color:'#9aa6b8'},ticks:{color:'#9aa6b8'},grid:{color:'#252d3b'}},y:{type:$('scale').value,title:{display:true,text:'Tempo (segundos)',color:'#9aa6b8'},ticks:{color:'#9aa6b8'},grid:{color:'#252d3b'}}},plugins:{legend:{labels:{color:'#eef2f8'}},tooltip:{callbacks:{label:c=>`${c.dataset.label}: ${c.parsed.y.toFixed(6)} s`}}}}});
}
function initCompare(){for(const el of [$('compareA'),$('compareB')])el.innerHTML=scenarios.map(s=>`<option value="${s.key}">${s.label}</option>`).join('');$('compareB').selectedIndex=1;$('compareN').innerHTML=[...new Set(rows.map(r=>r.n))].sort((a,b)=>a-b).map(n=>`<option value="${n}">${n.toLocaleString('pt-BR')}</option>`).join('');$('compareN').value='16000';updateCompare();}
function updateCompare(){const n=Number($('compareN').value);const a=$('compareA').value.split('|'),b=$('compareB').value.split('|');const ra=rows.find(r=>r.n===n&&r.algorithm===a[0]&&r.scenario===a[1]),rb=rows.find(r=>r.n===n&&r.algorithm===b[0]&&r.scenario===b[1]);$('nameA').textContent=$('compareA').selectedOptions[0].textContent;$('nameB').textContent=$('compareB').selectedOptions[0].textContent;$('timeA').textContent=ra?ra.time.toFixed(6):'—';$('timeB').textContent=rb?rb.time.toFixed(6):'—';$('comparisonText').textContent=ra&&rb?(ra.time===rb.time?'Os tempos registrados são iguais.':`${ra.time<rb.time?'A':'B'} teve menor tempo registrado nesta comparação (${Math.max(ra.time,rb.time)/Math.min(ra.time,rb.time).toFixed(2)}×).`):'Não há dados para essa combinação.';}
function quicksort(input,randomPivot){const a=input.slice();function partition(lo,hi){let p=lo;if(randomPivot){p=lo+Math.floor(Math.random()*(hi-lo+1));[a[p],a[lo]]=[a[lo],a[p]];}const pivot=a[lo];let i=lo+1;for(let j=lo+1;j<=hi;j++){if(a[j]<=pivot){[a[i],a[j]]=[a[j],a[i]];i++;}}[a[lo],a[i-1]]=[a[i-1],a[lo]];return i-1;}function sort(lo,hi){if(lo>=hi)return;if(hi-lo>800){throw new Error('Entrada muito grande para a execução recursiva segura neste navegador.');}const p=partition(lo,hi);sort(lo,p-1);sort(p+1,hi);}sort(0,a.length-1);return a;}
function runLab(){try{const raw=$('userInput').value.trim();let input;if(raw){input=raw.split(/[\s,;]+/).filter(Boolean).map(Number);if(input.some(v=>!Number.isFinite(v)))throw new Error('Use apenas números válidos separados por vírgulas ou espaços.');}else{throw new Error('Digite uma entrada ou clique em “Gerar entrada”.');}if(input.length<2)throw new Error('Informe pelo menos dois números.');if(input.length>20000)throw new Error('O limite é de 20.000 números.');const start1=performance.now();const fixed=quicksort(input,false);const t1=(performance.now()-start1)/1000;const start2=performance.now();const random=quicksort(input,true);const t2=(performance.now()-start2)/1000;$('fixedTime').textContent=t1.toFixed(6);$('randomTime').textContent=t2.toFixed(6);const ok=fixed.every((v,i)=>v===random[i]);$('fixedCheck').textContent=ok?'Saída ordenada e consistente':'Verifique o resultado';$('randomCheck').textContent=ok?'Saída ordenada e consistente':'Verifique o resultado';$('sortedOutput').textContent=fixed.slice(0,200).join(', ')+(fixed.length>200?` … (${fixed.length} valores no total)`:'' );$('labStatus').textContent=`Executados ${input.length.toLocaleString('pt-BR')} valores. Medição local do navegador; não compare diretamente com o CSV.`;if(labChart)labChart.destroy();labChart=new Chart($('labChart'),{type:'bar',data:{labels:['Pivô fixo','Pivô aleatório'],datasets:[{label:'Tempo (s)',data:[t1,t2],backgroundColor:[palette[0],palette[1]]}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{x:{ticks:{color:'#9aa6b8'},grid:{color:'#252d3b'}},y:{beginAtZero:true,title:{display:true,text:'Segundos',color:'#9aa6b8'},ticks:{color:'#9aa6b8'},grid:{color:'#252d3b'}}}}});}catch(e){$('labStatus').textContent=e.message;}}
function generate(){const count=Math.max(2,Math.min(20000,Number($('amount').value)||1000));const type=$('inputType').value;let a=Array.from({length:count},()=>Math.floor(Math.random()*100000));if(type==='sorted')a.sort((x,y)=>x-y);if(type==='reverse')a.sort((x,y)=>y-x);if(type==='duplicates')a=Array.from({length:count},()=>Math.floor(Math.random()*10));$('userInput').value=a.join(', ');$('labStatus').textContent=`Entrada de ${count.toLocaleString('pt-BR')} números gerada.`;}
$('algorithm').addEventListener('change',render);$('scenario').addEventListener('change',render);$('scale').addEventListener('change',render);$('reset').addEventListener('click',()=>{$('algorithm').value='all';$('scenario').value='all';$('scale').value='linear';render();});$('compareA').addEventListener('change',updateCompare);$('compareB').addEventListener('change',updateCompare);$('compareN').addEventListener('change',updateCompare);$('generate').addEventListener('click',generate);$('runLab').addEventListener('click',runLab);$('download').addEventListener('click',()=>{const blob=new Blob([['tamanho_n,algoritmo,cenario,tempo_segundos,razao_crescimento',...rows.map(r=>[r.n,r.algorithm,r.scenario,r.time,r.growth??''].join(','))].join('\n')],{type:'text/csv;charset=utf-8;'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='quicksort_resultados.csv';a.click();URL.revokeObjectURL(url);});
fetch(CSV_URL).then(r=>{if(!r.ok)throw new Error('Não foi possível carregar o CSV.');return r.text();}).then(text=>{rows=parseCSV(text);render();initCompare();}).catch(e=>{$('resultsBody').innerHTML=`<tr><td colspan="5">Erro ao carregar os dados: ${e.message}. Abra o site por um servidor local (por exemplo, Live Server).</td></tr>`;$('tableStatus').textContent='';});