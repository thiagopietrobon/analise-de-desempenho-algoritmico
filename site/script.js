const CSV_URL='../dados/quicksort_resultados.csv';
const $=id=>document.getElementById(id);
let rows=[],chart,labChart;
const palette=['#60a5fa','#4ade80','#fbbf24','#c084fc'];
if(typeof Chart!=='undefined')Chart.defaults.animation.duration=900;
function parseCSV(text){const lines=text.trim().split(/\r?\n/),headers=lines.shift().split(',');return lines.map(line=>{const v=line.split(','),o=Object.fromEntries(headers.map((h,i)=>[h,v[i]]));return{n:Number(o.tamanho_n),algorithm:o.algoritmo,scenario:o.cenario,time:Number(o.tempo_segundos),growth:o.razao_crescimento===''?null:Number(o.razao_crescimento)}})}
function filtered(){return rows.filter(r=>($('algorithm').value==='all'||r.algorithm===$('algorithm').value)&&($('scenario').value==='all'||r.scenario===$('scenario').value))}
function render(){const data=filtered();$('resultsBody').innerHTML=data.map(r=>`<tr><td>${r.n.toLocaleString('pt-BR')}</td><td>${r.algorithm}</td><td>${r.scenario}</td><td>${r.time.toFixed(6)}</td><td>${r.growth===null?'—':r.growth.toFixed(2)+'×'}</td></tr>`).join('');$('tableStatus').textContent=`Exibindo ${data.length} de ${rows.length} medições.`;if(typeof Chart==='undefined'){showError('A biblioteca dos gráficos não carregou.');return}const keys=[...new Set(data.map(r=>r.algorithm+'|'+r.scenario))],datasets=keys.map((key,i)=>{const[a,s]=key.split('|'),c=palette[i%palette.length];return{label:`Pivô ${a.toLowerCase()} · entrada ${s.toLowerCase()}`,data:data.filter(r=>r.algorithm===a&&r.scenario===s).sort((x,y)=>x.n-y.n).map(r=>({x:r.n,y:r.time})),borderColor:c,backgroundColor:c,tension:.2}});if(chart)chart.destroy();chart=new Chart($('resultsChart'),{type:'line',data:{datasets},options:{responsive:true,maintainAspectRatio:false,animation:{duration:900},interaction:{mode:'nearest',intersect:false},scales:{x:{type:'linear',title:{display:true,text:'Tamanho da entrada (N)'},ticks:{color:'#9aa6b8'},grid:{color:'#252d3b'}},y:{type:$('scale').value,title:{display:true,text:'Tempo (segundos)'},ticks:{color:'#9aa6b8'},grid:{color:'#252d3b'}}},plugins:{legend:{labels:{color:'#eef2f8'}},tooltip:{callbacks:{label:c=>`${c.dataset.label}: ${c.parsed.y.toFixed(6)} s`}}}}})}
function showError(m){const e=$('error');if(e){e.textContent=m;e.hidden=false}}
function quicksort(input,randomPivot){const a=input.slice(),stack=[[0,a.length-1]];while(stack.length){let[lo,hi]=stack.pop();while(lo<hi){let p=lo;if(randomPivot){p=lo+Math.floor(Math.random()*(hi-lo+1));[a[p],a[lo]]=[a[lo],a[p]]}const pivot=a[lo];let i=lo+1;for(let j=lo+1;j<=hi;j++)if(a[j]<=pivot){[a[i],a[j]]=[a[j],a[i]];i++}const k=i-1;[a[lo],a[k]]=[a[k],a[lo]];const left=[lo,k-1],right=[k+1,hi];if(left[1]-left[0]<right[1]-right[0]){if(right[0]<right[1])stack.push(right);[lo,hi]=left}else{if(left[0]<left[1])stack.push(left);[lo,hi]=right}}}return a}
function readInput(){const raw=$('userInput').value.trim();if(!raw)throw Error('Digite números ou gere uma entrada.');const a=raw.split(/[\s,;]+/).filter(Boolean).map(Number);if(a.some(x=>!Number.isFinite(x)))throw Error('A entrada contém valor inválido.');if(a.length<2)throw Error('Informe ao menos dois números.');if(a.length>20000)throw Error('Limite da medição: 20.000 números.');return a}
function generate(){const n=Math.max(2,Math.min(20000,Number($('amount').value)||16)),type=$('inputType').value;let a=Array.from({length:n},()=>Math.floor(Math.random()*100));if(type==='sorted')a.sort((x,y)=>x-y);if(type==='reverse')a.sort((x,y)=>y-x);if(type==='duplicates')a=Array.from({length:n},()=>Math.floor(Math.random()*10));$('userInput').value=a.join(', ');$('labStatus').textContent=`Entrada de ${n} valores gerada. Prepare a bancada para visualizar (máximo 80).`}
const labs={fixed:{events:[],index:0,timer:null,running:false},random:{events:[],index:0,timer:null,running:false}};
function makeSteps(input,randomPivot){const a=input.slice(),events=[];const record=(message,active=[],pivot=-1)=>events.push({a:a.slice(),message,active,pivot});function sort(lo,hi){if(lo>=hi)return;let p=lo;if(randomPivot){p=lo+Math.floor(Math.random()*(hi-lo+1));[a[p],a[lo]]=[a[lo],a[p]];record(`Pivô sorteado: ${a[lo]}.`,[lo,p],lo)}const pivot=a[lo];record(`Pivô escolhido: ${pivot}.`,[lo,hi],lo);let i=lo+1;for(let j=lo+1;j<=hi;j++){record(`Comparando ${a[j]} com pivô ${pivot}.`,[j],lo);if(a[j]<=pivot){[a[i],a[j]]=[a[j],a[i]];record(` ${a[i]} ≤ ${pivot}: elemento encaminhado à partição menor.`,[i,j],lo);i++}}const pi=i-1;[a[lo],a[pi]]=[a[pi],a[lo]];record(`Pivô ${pivot} fixado na posição ${pi}.`,[pi],pi);sort(lo,pi-1);sort(pi+1,hi)}sort(0,a.length-1);record('Ordenação concluída.',[], -1);return events}
function draw(side){const lab=labs[side],event=lab.events[lab.index],host=$(side+'Bars');if(!event){$(side+'State').textContent='Concluído';return}const min=Math.min(...event.a),max=Math.max(...event.a),range=max-min||1;host.innerHTML=event.a.map((v,i)=>{const active=event.active.includes(i),pivot=i===event.pivot;return `<div class="bar-item ${active?'is-active':''} ${pivot?'is-pivot':''}" style="height:${18+((v-min)/range)*78}%" title="Índice ${i}: ${v}"><span>${v}</span></div>`}).join('');$(side+'Event').textContent=event.message;$(side+'Count').textContent=`Passo ${lab.index+1} de ${lab.events.length}`;$(side+'State').textContent=lab.index===lab.events.length-1?'Concluído':lab.running?'Executando':'Pausado'}
function pause(side){const l=labs[side];l.running=false;if(l.timer)clearTimeout(l.timer);l.timer=null;if(l.events.length)$(side+'State').textContent='Pausado'}
function advance(side){const l=labs[side];if(!l.events.length)return;pause(side);if(l.index<l.events.length-1)l.index++;draw(side)}
function play(side){const l=labs[side];if(!l.events.length)return;if(l.index>=l.events.length-1)l.index=0;l.running=true;draw(side);const delays=[1400,950,600,320,120],delay=delays[Number($('speed').value)-1];function tick(){if(!l.running)return;if(l.index>=l.events.length-1){pause(side);draw(side);return}l.index++;draw(side);l.timer=setTimeout(tick,delay)}l.timer=setTimeout(tick,delay)}
function resetSide(side){pause(side);const l=labs[side];if(!l.events.length)return;l.index=0;draw(side)}
function prepare(){try{const input=readInput();if(input.length>80){$('labStatus').textContent='Para a animação, use no máximo 80 valores. A medição instantânea aceita até 20.000.';return}for(const side of ['fixed','random']){pause(side);labs[side].events=makeSteps(input,side==='random');labs[side].index=0;draw(side)}const start1=performance.now(),fixed=quicksort(input,false),t1=(performance.now()-start1)/1000;const start2=performance.now(),random=quicksort(input,true),t2=(performance.now()-start2)/1000;$('fixedTime').textContent=t1.toFixed(6);$('randomTime').textContent=t2.toFixed(6);const ok=fixed.every((v,i)=>v===random[i]);$('fixedCheck').textContent=ok?'Saída ordenada e consistente':'Resultado divergente';$('randomCheck').textContent=ok?'Saída ordenada e consistente':'Resultado divergente';$('sortedOutput').textContent=fixed.slice(0,200).join(', ');$('labStatus').textContent=`Bancada preparada com ${input.length} valores. Tempos medidos sem animação.`;if(typeof Chart!=='undefined'){if(labChart)labChart.destroy();labChart=new Chart($('labChart'),{type:'bar',data:{labels:['Pivô fixo','Pivô aleatório'],datasets:[{label:'Tempo (s)',data:[t1,t2],backgroundColor:[palette[0],palette[1]]}]},options:{responsive:true,maintainAspectRatio:false,animation:{duration:700},plugins:{legend:{display:false}},scales:{x:{ticks:{color:'#9aa6b8'}},y:{beginAtZero:true,title:{display:true,text:'Segundos'},ticks:{color:'#9aa6b8'}}}}})}}catch(e){$('labStatus').textContent=e.message}}
$('algorithm').addEventListener('change',render);$('scenario').addEventListener('change',render);$('scale').addEventListener('change',render);$('reset').addEventListener('click',()=>{$('algorithm').value='all';$('scenario').value='all';$('scale').value='linear';render()});$('generate').addEventListener('click',generate);$('prepareLab').addEventListener('click',prepare);$('speed').addEventListener('input',()=>{$('speedLabel').textContent=['Muito lenta','Lenta','Média','Rápida','Muito rápida'][Number($('speed').value)-1]});document.querySelectorAll('.step-run').forEach(b=>b.addEventListener('click',()=>play(b.dataset.side)));document.querySelectorAll('.step-pause').forEach(b=>b.addEventListener('click',()=>pause(b.dataset.side)));document.querySelectorAll('.step-next').forEach(b=>b.addEventListener('click',()=>advance(b.dataset.side)));document.querySelectorAll('.step-reset').forEach(b=>b.addEventListener('click',()=>resetSide(b.dataset.side)));
$('download').addEventListener('click',()=>{const blob=new Blob([['tamanho_n,algoritmo,cenario,tempo_segundos,razao_crescimento',...rows.map(r=>[r.n,r.algorithm,r.scenario,r.time,r.growth??''].join(','))].join('\n')],{type:'text/csv;charset=utf-8;'});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='quicksort_resultados.csv';a.click();URL.revokeObjectURL(url)});
fetch(CSV_URL).then(r=>{if(!r.ok)throw Error(`CSV não encontrado (HTTP ${r.status}).`);return r.text()}).then(t=>{rows=parseCSV(t);render()}).catch(e=>{$('resultsBody').innerHTML=`<tr><td colspan="5">Erro ao carregar: ${e.message}</td></tr>`;showError(e.message)});
generate();